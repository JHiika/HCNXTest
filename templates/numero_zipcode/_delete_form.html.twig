<form method="post" action="{{ path('app_numero_zipcode_delete', {'id': numero_zipcode.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ numero_zipcode.id) }}">
    <button class="btn">Delete</button>
</form>
